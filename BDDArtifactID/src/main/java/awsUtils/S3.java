package awsUtils;

public class S3 {

//geInstance()
	//getObject(bucketName, key)
	// deleteObject(bucketName, key)
	// deleteAllObjects(bucketname)
	// findKey(bucketname, subString);
	// getMeta(bucketname)
	
}
