package awsUtils;

public class DynaomDb {
	//getItem(tablename, primaryKey, primaryValue)
	// getItem(tablename, primaryKey, primaryValue, rangeKey, rangeValue)
	//scanspec()
	//deleteItem()
	//updateItem()
	// getAllItems()
	
}
